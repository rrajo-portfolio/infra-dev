apiVersion: 1

# El provisioning mantiene sincronizado el dashboard JSON bajo control de versiones.
providers:
  - name: portfolio-overview
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
